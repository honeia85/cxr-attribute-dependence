# Normalize line endings to LF in the repository (checked out native on each OS).
* text=auto eol=lf

# Treat these as binary (no line-ending conversion, no diff).
*.pdf binary
*.png binary
*.npy binary
*.npz binary
